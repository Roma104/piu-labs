import { initUI } from './ui.js';

document.addEventListener('DOMContentLoaded', () => {
    console.log('--- 1. APP: Inicjalizacja DOM OK. ---');
    initUI();
    console.log('Aplikacja Kształty uruchomiona.');
});
